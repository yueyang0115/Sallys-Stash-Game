int count  = 1;
while(count!=6){

scanner = system.in();
mystring = ...
inputhandler.bitcheck(mystring) //invalid 

if(bit_valid){  //board 'g'
 shapeadapter.locationcheck(x, y, height,width,board); //invalid
 if(location_valid){
   Rectangle myrectangle = new Rectangle(height,width,'d',id)
   myrectangle.putonboard(int x, int y, Board board)
   id+=;
 }
}

}

  
